services:
  mock-api:
    build: .
    ports:
      - "5080:8080"
      - "5443:8443"
    volumes:
      - ./EggIncognito/Endpoints:/app/Endpoints
      - ./certs:/app/certs:ro
      - /var/run/usbmuxd:/var/run/usbmuxd
    environment:
      - EndpointsPath=/app/Endpoints
