@rendermode @(RenderModes.InteractiveServer)
@using EggIdentity.Settings.AdminUi
@inject ICurrentUser User

<div class="admin-pane-head">
    <h2 class="admin-pane-title">Settings</h2>
    <span class="text-muted text-xs">Database-backed configuration. Bootstrap keys stay on the stack.</span>
</div>
<SettingsPanel UpdatedBy="@(User.Username ?? User.DiscordId)"/>
