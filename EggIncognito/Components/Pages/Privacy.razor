@page "/privacy"
@rendermode @(RenderMode.InteractiveServer)
@inject NavigationManager Nav

@code {

    protected override void OnAfterRender(bool firstRender) {
        if (!firstRender) return;
        Nav.NavigateTo("/protos/privacy", replace: true);
    }

}
