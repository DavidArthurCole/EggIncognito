@page "/docs"
@inject NavigationManager Nav

@* Docs live in the API workbench at /protos#api/docs. This legacy route redirects so old links
   keep working. *@

@code {

    protected override void OnInitialized() {
        Nav.NavigateTo("/protos#api/docs", replace: true);
    }

}
