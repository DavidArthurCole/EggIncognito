@page "/docs"
@inject NavigationManager Nav

@* Docs live in the API workbench at /protos#api (endpoint Docs tab + the Objects view). This
   legacy route redirects so old links keep working. *@

@code {

    protected override void OnInitialized() {
        Nav.NavigateTo("/protos#api", replace: true);
    }

}
