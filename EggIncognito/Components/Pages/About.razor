@page "/about"
@inject NavigationManager Nav

@* About content folded into the homepage footer. This legacy route redirects so old links keep working. *@

@code {

    protected override void OnInitialized() {
        Nav.NavigateTo("/", replace: true);
    }

}
