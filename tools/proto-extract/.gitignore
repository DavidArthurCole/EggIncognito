# Heavy third-party binaries (38MB: dex2jar, protoc, jad). Placed by setup.sh / the operator,
# not version-controlled. Mirrors the tools/tailwind/ fetch posture.
pbtk/utils/external/

# Local venv created by setup.sh.
.venv/

# Stray python bytecode.
__pycache__/
*.pyc
